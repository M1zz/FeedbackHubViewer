# todo

## ASO 점검과 키워드 수정안 (2026-09-24, App Store kr · us 검색 상위 200위 기준)
- [x] 스토어에 나가 있는 앱 60개의 키워드 필드(한국 210개 · 미국 93개)를 전부 검색해 순위를 잼
- [x] 공통: 쉼표 뒤 공백 · 이름/부제와 겹치는 단어로 앱마다 3~20자 낭비. 키워드 필드가 빈 앱 15개
- [ ] 클립키보드 ko — 부제 `상용구·자주 쓰는 문구 빠른 입력`, 키워드 `문구저장,복붙,미리 작성된 답변,짧은 문구,빠른 답장,자동입력,클립보드 관리,템플릿,붙여넣기,단축문구,노마드,프리랜서,해외송금,송장,이메일,고객응대,서명`
- [ ] Clip Keyboard en-US — 부제 `Text Expander & Quick Replies`, 키워드 `snippets,canned,responses,reply,saved,clipboard,manager,paste,autotext,shortcuts,templates,email`
- [ ] 두번알림 ko — 키워드 `발표 타이머,회의 타이머,예비 알림,멘토링 타이머,스피치,강의,수업,면접,발표연습,남은시간,프레젠테이션,강연,설교,토론,미리알림,진동`
- [ ] Rereminder en-US — 부제 `Speech & Presentation Timer`, 키워드 `talk,presenter,speaker,lecture,meeting,countdown,pre-alert,vibration,class,sermon,debate,warning`
- [ ] StickyPresenterRemote ko — 부제 `발표 타이머 · 슬라이드 리모컨`, 키워드 `발표 리모컨,프레젠테이션,PPT,키노트,프레젠터,발표도구,발표연습,원격,애플워치,강의,회의,스티키` (PPT · 키노트 · 애플워치 지원 여부 확인)
- [ ] 달빛 ko — 키워드 `화이트 노이즈,마음의 안정,안식,평안,싱잉볼,평온,숙면,불면증,유도,진정,빗소리,파도소리,수면유도,잠잘때,자장가,명상음악` (빗소리 이하 검증 안 함)
- [x] 새 버전 만듦(App Store Connect, 제출 준비 중): 클립키보드 5.1.6 · 두번알림 2.2.9 · StickyPresenterRemote 1.0.9. 달빛은 4.1.1이 이미 있었음
- [x] 수정안 입력(사용자 확인 후): 부제+키워드 전부, StickyPresenterRemote는 PPT·키노트·애플워치 뺌. 6개 로케일 저장 · 재확인 완료
- [ ] 각 앱 리포의 MARKETING_VERSION을 새 버전에 맞춰 빌드를 올려야 제출 가능
      (로컬 Rereminder 리포는 2.2.5 — 스토어는 2.2.8이라 리포가 뒤처져 있음)
- [ ] 출시 1~2주 뒤 순위 추이 · 노출 · 전환으로 효과 확인

## App Store Connect 연결을 설정으로 (2026-09-24)
- [x] "연결 안 됨"이던 까닭: 앱 키체인에 키가 없음(스크립트로만 확인했음). 연결 폼을 설정으로 옮김 —
      맥은 설정 창(⌘,), 아이폰 · 아이패드는 "더 보기 → 설정" 시트. 앱 내 구입 · 키워드 카드에는 "설정 열기" 버튼
- [ ] 사용자가 설정에서 키를 넣고 저장되는지 확인

## 프로젝트 목록을 DAU 순으로 (2026-09-24)
- [x] 사이드바 · 휴대폰 프로젝트 목록 정렬: 7일 사용 건수 → **어제 DAU**(어제 이벤트를 보낸 서로 다른 설치).
      동률이면 7일 사용자 → 7일 사용 건수 → 설치 → 피드백. 오늘이 아니라 어제인 까닭: 끝나지 않은 하루로 세우면 아침마다 뒤집힘
- [x] 사이드바 줄 오른쪽에 "DAU N"(안 읽은 피드백 뱃지는 옆에). 휴대폰 카드의 "어제 사용(건)" 칸을 "DAU (어제)"로
- [ ] 화면에서 눈으로 확인 못 함

## ASO 강화 (2026-09-24)
- [x] **순위 추이** 카드: 순위 높은 키워드 6개를 30/90일 차트로(1위가 위). 일주일 전과 견줘
      5계단 넘게 오르내림 · 순위에서 빠짐 · 새로 잡힘 · **나를 새로 앞지른 앱**
- [x] **스토어 메타데이터** 카드: App Store Connect에서 로케일별 이름 · 부제 · 키워드 · 프로모션 텍스트.
      키워드 필드 점검(쉼표 옆 공백 · 중복 · 이름/부제에 이미 있는 단어 → 정리안과 확보 글자 수),
      추적 키워드가 이름/부제/키워드 중 어디에 있는지(나라 → 로케일). 라틴 문자는 단어 단위, 한글은 부분 문자열
- [x] **메타데이터 고치기**: 바뀌는 것(전/후)을 보여 준 뒤 저장. 키워드 · 이름 · 부제는 고칠 수 있는
      버전이 있을 때만, 프로모션 텍스트는 나가 있는 버전에서 바로
- [x] **노출 · 전환** 카드: App Store 분석 리포트(Discovery and Engagement · Downloads)로 경로별
      노출 → 페이지 조회 → 첫 다운로드, 전환율, 날짜별 검색 노출 · 첫 다운로드 차트. 리포트 요청이 없으면 만들기 버튼
- [x] 같은 날짜가 연속된 두 리포트 파일에 겹쳐 온다 — 날짜마다 늦게 가공된 파일만 씀.
      안 그러면 두 배로 셌다(판매 리포트 7,659 ↔ 고친 뒤 7,640)
- [x] 실측(클립키보드): ko 키워드 필드 공백 16자 + "키보드" 중복 → 20자 확보, en-US는 이름·부제와 겹치는 단어로 37자 확보.
      첫 다운로드가 8/27 하루 2,646건 정점 → 지금 하루 1~4건, 검색 노출 하루 3천대 → 50~80
- [x] 저장(PATCH) 확인 — 2026-09-24 네 앱 새 버전에 키워드 · 부제를 넣고 다시 읽어 값이 같음을 확인
- [ ] 화면을 띄워 눈으로 확인 못 함(빌드 · 스크립트로 API 응답만 확인)
- [ ] 노출 리포트는 앱을 열 때마다 30~60초 걸려 받음(이번 실행 동안만 기억) — 디스크 캐시 필요하면 추가

## 앱 내 구입 섹션 — App Store Connect API (2026-09-24)
- [x] 새 섹션 "앱 내 구입": 앱마다 인앱 상품·구독(이름 · 상품 ID · 상태 · 가격)과 최근 30일 결제 건수 · 수익금 · 첫 다운로드
- [x] 전체 프로젝트: 인앱 결제 · 첫 다운로드가 많은 앱 순위
- [x] 키(Issuer ID · Key ID · .p8 · 판매자 번호)는 키체인에만. 저장 전에 개인 키가 읽히는지 확인
- [x] 맥 엔타이틀먼트에 files.user-selected.read-only 추가(.p8 파일 불러오기)
- [x] 빌드(macOS · iOS 시뮬레이터), gzip 풀기 · 리포트 파싱 · 키 검증은 스크립트로 확인
- [x] 실제 키로 확인(스크립트, 2026-09-24): 클립키보드 상품 6개 · 원화 가격(Pro ₩17,000 등) · 30일 판매 리포트
- [x] 가격: 인앱은 한국 수동 가격 → 한국 자동 환산 가격 → 기준 국가, 구독은 한국 → 미국
- [x] 0원 줄(오퍼 · 프로모션 코드, 주문 유형 GIFT)을 결제에서 빼고 "코드"로 따로 셈. 코드 이름별 건수도 적음
- [ ] 실측: 클립키보드 최근 30일 첫 다운로드 7,659 · **유료 결제 0건** · 코드로 0원 Pro 6,643건(LEEO 6,093 · LEEO2 550).
      통계의 "유료기능 5,719명 · 무료기능 36명"이 거의 다 이 코드에서 왔다는 뜻 — 잠재 고객(안 열린 설치)이 거의 없음
- [ ] 앱 화면에서 키를 넣고 눈으로 확인 — 스크립트로만 확인함(앱 키체인에는 사용자가 직접 넣어야 함)
- [ ] 어제 리포트가 아직 안 나온 날(404)은 판매 0인 날과 구분 안 됨
- [ ] 다음: 앱이 보낸 산 설치(own.*)와 스토어 판매를 나란히 대조, 조작(가격 · 상품) 기능

## 잠재 고객 = 유료 기능이 안 열린 설치 전부 (2026-09-24)
- [x] 잠재 결제 고객 카드(wall): 결론 문장이 잠재 고객 총수부터 말하고, 한도 지표를 안 보낸 무료 설치를 "모름"에서 "한도까지 거리 모름"(잠재 고객)으로 옮김
- [x] 전체 프로젝트 비교: "잠재 고객이 많은 앱" 순위 카드와 1위 타일 추가
- [ ] ClipKeyboard 앱 리포 스펙(docs/engineering/usage-spec.json)의 wall note가 옛 정의("한도에 닿았는데 안 열린 사람")를 말함 — 앱 리포에서 고치고 sync해야 함
- [ ] spec-sources.sh의 앱 리포 경로가 workspace/Auto/…인데 이 기기엔 code/ClipKeyboard · code/Rereminder에 있음 — 이 기기에선 드리프트 검사가 건너뛰어짐

## 전체 프로젝트 통계 = 앱끼리 비교 (2026-09-24)
- [x] 전체 프로젝트 탭의 통계를 합산 대시보드에서 **앱별 순위**로 바꿈(`ProjectComparisonView`).
      1위 타일 넷(설치 · MAU · 7일 신규 · 고착도) + 순위 카드 여섯(설치 · MAU · 7일 신규 · 고착도 · WAU 증감 · 피드백)
- [x] 고착도는 MAU 20명 미만 앱을 줄 세우지 않음(한두 사람이 순위를 뒤집음). 값을 말할 수 없는 앱은 0이 아니라 줄에서 뺌
- [x] 맥에서는 순위 줄을 누르면 그 앱 통계로 이동
- [x] macOS · iOS 시뮬레이터 빌드 성공
- [ ] 화면을 띄워 눈으로 확인하지 못함
- [ ] 앱 전체가 한국어 하드코딩이라 String Catalog(영어)는 이 화면만 따로 붙이지 않음 — 앱 단위로 결정 필요

## 산 것으로 다시 쌓기 — 규약 2 (2026-09-19)
- [x] 원인: 두번알림 `flag.isPro` = `StoreManager.isProUser` = 맥(항상 true) ∪ TestFlight·샌드박스 ∪ 그랜드파더 ∪ 실제 구매.
      스펙이 이 값을 "이미 결제한 사람" 타일과 "결제함" 세그먼트로 읽어서 아무도 안 샀는데 26대가 결제로 잡혔다
- [x] **초기화는 지우지 않고 기준선으로**: 앱이 `flag.schema = 2`를 보내고, 뷰어는 스펙 `purchases.since` 이상인
      설치만 산 것으로 가른다. 그 전 버전은 모름. CloudKit 원본·로컬 캐시는 손대지 않음(사용 기록은 멀쩡하다)
- [x] **산 것마다 한 칸(`own.*`), 칸은 2ⁿ** — 결제 축 여섯 칸(Pro 결제·옛 유료·부가·체험·무상·안 냄)을 걷어냈다.
      체험·무상·옛 유료 다운로드는 칸이 아니라 "돈 없이 열림" 표식. 프로젝트 하나를 볼 때만 뜬다. 30대 미만 칸은 흐리게
- [x] ClipKeyboard 앱: `own.pro` · `own.slots`(팩 수) · `own.twodevice` · `flag.schema` (TestFlight는 0) — 8칸
- [x] 두번알림 앱: `own.pro` · `flag.isPaid` · `flag.isComped` · `flag.schema` (샌드박스는 0) — 2칸.
      스펙의 타일·세그먼트를 `own.pro`로, `flag.isPro` 라벨을 "Pro가 열린 설치 (결제 아님)"로
- [x] 뷰어·두 앱 빌드 확인, 스펙 해석·칸 차례는 스크립트로 확인
- [ ] 두 앱을 새 버전으로 내보내야 칸이 차기 시작한다 — 그 전까지 산 것 카드는 전부 0대(모름만 있음)
- [ ] 화면을 띄워 눈으로 확인하지 못함(실행 중인 뷰어를 안 끄려고)
- [x] 뷰어(dev) 커밋·푸시 (2026-09-24). ClipKeyboard(spec/monetization-clause) · Rereminder(spec/cards-migration)는 아직

## 앱 이름을 스토어 이름으로 (2026-09-17)
- [x] **앱이 보낸 이름 대신 App Store 이름** — 우선순위: 수동 지정 → 스토어 이름 → 앱이 보낸 이름 → 번들 ID.
      키워드 화면이 이미 잇는 번들 ID ↔ 스토어 링크(`keywords.json`)를 재사용한다(`FeedbackStore.storeAppNames`).
      바뀌는 앱: ClipKeyboard→클립키보드 · Rainbow Workshop→무지개 공방 · 일정 밀도→욕망의 무지개 ·
      Goldweek→골드위크 - 황금연휴 플래너 · 달빛→달빛 - 수면 백색 소음 & 자연의 소리 · Rebound Journal→징검돌
- [x] 이어진 앱의 스토어 이름·아이콘을 **하루 한 번** 다시 읽는다 — 링크는 한 번 이으면 안 물어서
      이름을 바꾼 앱이 옛 이름으로 남았다(캐시의 "벅터벅터", 스토어는 지금 "벅뚜벅뚜")
- [ ] (결정) 스토어 이름에 붙은 부제 꼬리(" - 황금연휴 플래너")를 사이드바에서 자를지 — 지금은 스토어 그대로
- [ ] 화면에서 눈으로 확인 못 함(실행 중인 뷰어를 안 끄려고). 빌드 + 캐시 데이터로 매핑만 확인

## 결제 종류로 보기 (2026-09-17)
- [x] **무리 고르개에 결제 축을 둘째 줄로** — Pro 결제 · 옛 유료 구매 · 부가 결제 · 체험 · 무상 · 안 냄.
      접근 축(유료기능·무료기능)과 따로 선다: 결제 키(`flag.isPaid`)를 보내는 범위에서만 뜨고,
      안 보내는 설치는 "안 냄"이 아니라 모름. 한 설치는 한 칸 — 돈에 가까운 쪽이 이긴다
- [x] **결제 종류 카드** — 칸마다 전체·최근 30일 활성 수와 비중. 전체를 고른 동안만
- [x] 스펙에 `addOnFlag`(규약 `flag.boughtAddOn`) · `legacyPaidFlag`(규약 `flag.isLegacyPaid`) —
      "옛 유료 구매" 칸이 Pro 결제와 갈린다. 둘 다 늦게 생긴 키라, 스펙에 적혀 있으면 아직 아무도
      안 보내도 고르고 안 보낸 설치 수를 각주에 적는다(안 냄 ← 부가 결제, Pro 결제 ← 옛 유료 구매)
- [ ] ClipKeyboard 실측(캐시 기준): 5,763대 중 결제 키를 보내는 설치 157대(Pro 결제 116 · 무상 41).
      Pro 결제 116대는 전부 옛 유료 구매 키를 안 보내는 버전이라 각주가 붙는다. 새 버전이 퍼지면 갈린다
- [ ] 화면을 띄워 눈으로 확인하지 못했다(실행 중인 뷰어를 끄지 않으려고). 빌드와 캐시 데이터 계산만 함

## 완료
- [x] **두번알림 스펙을 `cards` 형식으로**: 옛 절 넷(tileGroups·distributions·segments·funnels)을
      카드 8장으로 옮기고 결제와의 거리로 묶었다 — **결제**(결제 준비도 · 결제 퍼널 · 알림 개수 수요)
      → **가치 경험**(첫 가치 경험 · 핵심 가치 · 완주 분포) → **사람**(사용자 구분 · 기능 채택률).
      옛 형식에서는 앱이 순서를 못 정해 타일 셋이 먼저 나오고 퍼널이 맨 끝이었다. 내용은 한 줄도
      안 바뀌었고(타일 4·5·4개, 세그먼트 규칙 8개, 퍼널 4·2단계, 분포 6·6구간, derived 3개 그대로)
      묶음 차례만 스펙이 정한다
- [x] **`wall` 카드가 지표를 안 보낸 설치를 0으로 읽던 것**: "아직 시작 안 함"으로 세고 있었다.
      안 보낸 것과 0인 것은 다른 말인데, 한도 지표를 새 버전만 보내는 앱에서는 그 차이가 설치의
      95%가 되기도 한다(두번알림의 `alertsMax`가 5.5%). 이제 모름으로 빠지고, 모름 줄이 이유를
      갈라 적는다(권한을 안 보냄 N대 · 지표를 안 보냄 M대). 한도 지표를 절반도 못 받으면
      카드가 스스로 "이 숫자는 하한"이라고 말한다

## 두번알림에 아직 못 붙이는 것
- [ ] **`wall`(잠재 결제 고객) 카드** — 이 앱의 무료 한도는 알림 1개이고 그걸 재는 지표는
      `alertsMax`인데, 2,397대 중 133대(5.5%)만 보낸다. 무료기능 설치 1,835대에서는 13대(0.7%).
      지금 붙이면 카드의 99%가 "모름"이라 볼 것이 없다. 앱이 `alertsMax`를 전 버전에서 보내기
      시작하면 스펙에 카드 한 장(`{ "kind": "wall", "metric": "alertsMax", "limit": 1 }`)만 더하면 된다
- [ ] (선택) `ladder`(활성화 사다리) — 이 앱의 쐐기는 '타이머 완주'인데 `timerCompletions`도
      커버리지가 낮다(무료기능 1,835대 중 9대). 같은 이유로 보류
- [x] **앱마다 다른 대시보드를 스펙만으로 구성**(`ProjectStatsSpec+Dashboard.swift`):
      예전에는 카드의 종류도 차례도 묶음도 Swift에 박혀 있었다. `tileGroups`를 먼저 그리고 그 다음
      `distributions`… 하는 식이라 앱이 자기 화면의 순서를 정할 수 없었고, 새 종류 하나에 스펙·평가·
      뷰 세 파일을 함께 고쳐야 했다. 이제 카드를 한 줄로 세운다:
      · `"cards": [{ "kind": …, "section": …, "title": … }]` — 적은 차례가 그리는 차례,
        `section`이 같은 카드끼리 묶인다. 겉 정보(title·note·icon·section)는 모든 종류가 같은 자리
      · 종류 여덟: tiles · distribution · share · segments · funnel · ladder · wall · moments
      · **옛 스펙은 한 글자도 안 고쳐도 된다** — `cards`가 없으면 옛 절들을 지금까지의 차례로 푼다.
        옛 절은 *짧게 쓰는 법*이지 다른 기능이 아니라, 두 길이 같은 곳으로 모여 화면에 분기가 없다.
        `monetization` 절 = ladder+wall+moments 세 장을 "수익" 묶음에 넣은 것
      · **모양은 안 늘린다**(tiles · bars · funnel 셋). 앱마다 모양이 달라지면 두 앱을 나란히 못
        읽는다. 새 종류는 셋 중 하나로 번역돼야 하고, 그래서 뷰는 안 고친다
      · 입력은 `Context` 자루 하나로 모았다(지표·이벤트·건수 가능 여부·설치별 권한). 어제 수익
        카드가 권한을 필요로 해서 스펙 밖·스토어 안에 따로 살았던 이유가 이거였다 — 이제 없다
      · 스펙은 **색이 아니라 뜻**을 적는다(`normal`·`hot`·`warn`·`muted`). 앱이 색을 고르면
        같은 뜻이 앱마다 다른 색이 된다
      실측 검증: ClipKeyboard 9장(수익 3 + 나머지 6) · 두번알림 8장이 옛 스펙 그대로 나오고,
      새 `cards` 형식으로 쓴 스펙도 묶음 차례(수익 → 쓰임새 → 안 묶임)까지 그대로 나온다
- [x] **깨진 스펙을 조용히 버리던 것**(`ProjectStatsSpecCatalog.load`): `try?` 하나로 묻혀서,
      오타 한 글자에 그 앱 대시보드가 통째로 사라지고 화면에는 "스펙이 아직 없습니다"가 떴다.
      **없는 것과 깨진 것은 할 일이 정반대인데**(쓰는 일 / 고치는 일) 같아 보였다. 이제 파일 이름과
      고칠 수 있는 이유가 카드로 뜬다 — `cards[0] 에 "limit" 가 없습니다`,
      `Cannot initialize CardKind from invalid String value leaderboard`.
      스펙이 아닌 JSON은 `appId` 유무로 걸러 경보가 소음이 되지 않게 했다
- [x] **수익 관측 대시보드**(`FeedbackStore+Monetization.swift` · `MonetizationCards.swift` ·
      스펙의 `monetization` 절): 가격표가 맞는지가 아니라 **가격표가 서 있을 땅이 있는지**를 잰다.
      값은 스펙에 안 적는다 — App Store Connect가 진실이고 뷰어에는 대조할 상대가 없어서, 적어 두면
      숫자만 그럴듯해진다. 카드 셋:
      · **활성화 사다리(쐐기)**: 설치 → 지표 조건들. 앱이 스스로 정한 목표·하한을 선으로 긋는다.
        선이 있어야 7%가 "낮다"가 아니라 "하한의 7분의 1"로 읽힌다. 제일 크게 새는 칸도 찾아 적는다
      · **잠재 결제 고객**: 결제가 필요해지는 자리는 기능이 아니라 **한도**다. 그래서 잠재 고객은
        "기능을 안 쓰는 사람"이 아니라 "한도에 닿았는데 아직 안 열린 사람". 지금 막힘 · 곧 막힘 ·
        아직 여유 · 시작 안 함 · **팔 대상 아님(이미 열림)** · 모름으로 가른다. 이미 열린 설치는
        값을 낼 이유가 없어 모수에서 빠지고, 그 칸이 크면 카드가 "가격표보다 권한을 보라"고 말한다
      · **결제 화면이 뜬 순간**: 설계한 벽마다 노출·누름·결제를 설치 수로. 한 건도 없는 벽은
        "아무도 안 왔다"가 아니라 **"벽이 아직 안 섰다"**로 적는다 — 고칠 곳이 가격이 아니라 코드다.
        누름·결제는 그 벽을 만난 설치와의 **교집합**이라 다른 벽의 결제가 섞이지 않는다
      전부 설치 단위다(건수로 세면 페이월을 열 번 본 한 사람이 열 명이 된다). 무리 고르개와 무관하게
      언제나 전체 설치를 본다 — "유료기능만 놓고 본 잠재 고객"은 정의상 0이다

## ClipKeyboard 실측이 말한 것 (2026-09-17, 설치 5,756대)
- [ ] **활성화율 7.2%** — 키보드를 실제로 쓴 설치는 413대뿐이다. 단축어는 98.5%가 갖고 있는데
      (샘플이 심어져 있어서다) 키보드까지 가는 건 그중 7.3%. 수익 문서는 50%를 가정하고 65%를
      목표로 잡았지만, 실제는 `KILL_CRITERIA.md`가 제품 문제로 못 박은 50%의 7분의 1이다.
      **가격표를 고치기 전에 여기다.** 문서 7절의 결론("활성화율 15%p가 가격표보다 크다")이
      실측에서는 훨씬 더 크게 맞는다
- [ ] **팔 수 있는 대상이 1명** — 한도(10칸)에 닿았는데 안 열린 설치가 1대, 곧 닿을 설치가 0대.
      5,720대(99.4%)는 이미 열려 있어 값을 낼 이유가 없다. 원인은 `ProStatusManager`의
      `bootstrapV4GrandfatherFlags`: 메모가 하나라도 있으면 `existingFreeUser`를 켜는데, 신규
      설치에도 샘플이 심어져 있어 **갓 설치한 기기까지 v3.x 기존 유저로 잡힌다**(실행 2회 이하
      15대가 전부 `isComped=1`). 권한을 건드리는 일이라 뷰어에서 고칠 수 없다 — 앱에서 판단할 것.
      이걸 안 고치면 아래 가격 작업은 전부 1명을 위한 것이 된다
- [ ] **설계한 벽이 거의 안 선다** — 5,756대 중 페이월을 본 설치가 5대. 순간 지도 10개를 만들어도
      뜰 일이 없다(위 두 항목의 결과다). 벽을 늘리기 전에 벽에 닿는 사람을 만들어야 한다
- [ ] 수익 문서가 요구한 측정 중 아직 안 오는 것: `onboarding_completed`(스펙에 라벨만 있고 한 건도
      안 옴 → 온보딩 칸을 사다리에 못 넣음), `offer_code_redeemed`(0건), 결제 시점의
      `prior_purchase` 파라미터(업그레이드 가설의 유일한 검증 수단). 앱에서 보내면 대시보드가 바로 받는다
- [ ] 가격표 사다리(칸 추가 소모성 3회 · 업그레이드 SKU · 두 대째)는 위 두 개가 정리된 뒤에.
      지금 붙이면 모수가 1명이라 어느 가설도 6주 안에 답이 안 나온다
- [x] **무리를 셋으로 정리: 전체 · 유료기능 · 무료기능**(`FeedbackStore+Audience.swift`).
      이 화면은 두 번 틀렸고 두 번 다 같은 이유였다 — 가진 값에 안 맞는 질문을 물었다. 앱이 보내는
      0/1이 실제로 재는 값은 "지금 유료 기능을 쓸 수 있는가"지 "돈을 냈는가"가 아니다. 결제로
      읽으면 신규 설치의 99%가 유료가 되고(1차 사고), 결제만 물으면 8,201대 중 5,726대가 모름이
      된다(2차). 질문을 값에 맞추니 둘 다 아니다 — **모름 52대(0.6%)**.
      · 옛 이름(`flag.isPro` 등)은 버린 게 아니라 제자리를 찾아 줬다 — 결제 자리에선 거짓말이지만
        "쓸 수 있는가" 자리에선 참이다. 규약 셋(`isPaid`·`isTrial`·`isComped`)도 전부 "열렸다"의
        근거로만 쓴다(권한은 본래 합집합)
      · 권한은 **합집합**이라 한 앱에 구버전·신버전이 섞여도 설치마다 제가 보낸 것으로 판정된다 —
        앱의 배포 상태가 숫자를 흔들지 않는다
      · 결제·체험·무상 칸과 거기 딸린 경보는 걷어냈다. 그 셋을 말해 주는 앱이 거의 없어 대부분
        빈 칸이거나 "아는 일부만 놓고 센 값"이라는 단서가 줄줄이 붙었다 — 고르개는 고를 수 있는
        것만 있어야 한다. 칸이 앱에 따라 나타났다 사라지지 않으므로 프로젝트를 옮겨도 고른 것이 남는다
      · 실측 검증: ClipKeyboard 유료기능 5,719 / 무료기능 36 / 모름 0(99%), 두번알림 535 / 1,852 / 4(22%),
        전체 6,254 / 1,895 / 52(77%). 세 범위 × 세 창 아홉 칸 모두 `유료기능+무료기능+모름 = 타일`
      · 근거: ClipKeyboard 무료 한도(단축어 10개)를 넘긴 65대는 전부 `flag.isPro`가 켜져 있었고,
        한도 안에서 쓰는 5,690대 중에서도 5,654대가 켜져 있었다. 결제라면 설명이 안 되지만
        "쓸 수 있는가"라면 설명이 된다 — 5.0.0부터 모두에게 열어 둔 것이다
- [x] 퍼널 각주에 마크다운 별표가 그대로 보이던 것(`Text(String)`은 마크다운을 안 파싱한다)
- [x] **카드의 산수가 안 닫히던 것**: 권한을 아예 못 읽는 앱의 설치를 통째로 건너뛰어서
      (`guard let entitlement ... else { continue }`), 전체 8,201대인데 갈린 것과 모름을 다 더해도
      8,153대뿐이었다 — 사라진 48대가 어디로 갔는지 화면 어디에도 없었다. 각주에 "빠져 있다"고
      적어 두긴 했지만 부족하다: 맞지 않는 숫자를 본 사람은 각주를 읽는 게 아니라 화면 전체를
      의심한다. 이제 그런 설치도 모름으로 센다
- [x] **표본이 얇으면 비중을 아예 말하지 않는다**(`Slice.isTooThin`): 권한을 보내는 설치가 그 창의
      절반에 못 미치면 막대와 퍼센트 자리에 `—`를 두고 셈만 적는다. 규약을 막 올린 앱에서 권한을
      보내는 설치는 "먼저 업데이트한 사람들"이지 무작위 표본이 아니다. 못 그린 줄에는 왜 못 그렸는지가
      셈보다 먼저 온다 ("5,755대 중 81대만 권한을 보내 비중은 아직 모릅니다") — 셈만 있으면
      이 앱에 그런 사용자가 없다는 말로 읽힌다
- [x] **어떤 기기에서도 양옆이 안 잘리게**(큰 글씨·좁은 화면): 화면이 잘린 게 아니라 내용이 화면보다
      넓었다. `HStack`은 아이들이 요구하는 너비의 합을 제 너비로 삼고, 그 줄이 부모(세로 스택)의
      너비가 되어 화면 가운데 놓이므로 **좌우가 똑같이** 잘려 나갔다. 너비를 우기던 것들을 다 고쳤다:
      · 섹션 칸(피드백·통계·진단·키워드)을 `HStack` → `LazyVGrid`. 격자는 받은 너비를 절대 넘지 않고,
        접근성 글씨 크기에서는 두 칸씩 두 줄로 접는다. 안 읽음 뱃지는 아이콘 **옆이 아니라 자리에** —
        칸 하나에 줄지 않는 조각(아이콘·뱃지)이 둘이면 줄어드는 것은 이름뿐이라 "피드백"이 "피…"가 된다
      · `Tag`·`FilterChip`·`StatusChip`의 `fixedSize()` 제거 → 자리가 없으면 줄여 쓰다 잘라 쓴다.
        태그는 아이콘부터 버리고(값이 내용, 아이콘이 장식), 상태 칩은 반대로 이름부터 버린다(✓가 내용)
      · `FlowLayout`: 줄보다 넓은 조각(긴 이메일 태그 하나)이 줄 너비를 화면 밖까지 늘리던 것 —
        그런 조각에는 줄 너비를 제안해 그 안에서 접게 한다
      · 피드백 목록 머리줄: `정렬·필터`의 `fixedSize()` 제거 + 좁아지면 이름을 접고 아이콘만,
        `모두 읽음`은 큰 글씨에서 아이콘을 버리고 두 줄까지 접힘, 행의 처리 버튼은 54pt 고정 →
        최소 크기(큰 글씨에서 동그라미가 잘리던 자리)
      · `Figure`의 이름을 두 줄까지 — "아직 나는 이슈"가 "아직…"이 되면 무엇을 센 숫자인지 사라진다
      시뮬레이터에 실제 캐시를 넣고 기본 크기 · 접근성 XL · 접근성 XXXL에서 목록·통계·진단·키워드·상세를
      모두 확인했다(이제 어느 크기에서도 화면 밖으로 나가는 것이 없다)

- [x] 아이폰에서 **크래시 이슈 상세의 양옆이 잘리던 것**(`CrashIssueDetailView`): 시트에
      `frame(minWidth: 460)`이 플랫폼 구분 없이 걸려 있었다. 맥의 시트는 제 크기를 스스로 정하니
      최소 너비가 필요하지만, 아이폰의 시트 너비는 화면이 정한다 — 460은 아이폰(390~430)보다 넓어
      시트가 넓어지는 대신 내용이 가운데 놓인 채 좌우가 똑같이 잘렸다. 최소 크기는 맥에만 준다

- [x] 새로 들어온 앱의 아이콘이 점선으로 남던 것(`KeywordStore.syncLinks`): 스토어 링크를 실행할 때
      **한 번만** 풀고 있었다. 앱이 처음 리포트를 보내면 그 앱은 새로고침이 끝난 뒤에야 목록에 들어오는데,
      링크는 그보다 몇 분 앞서 이미 풀린 뒤라 카드만 생기고 아이콘은 "App Store에 없음"을 뜻하는 점선
      자리표시자로 다음 실행까지 남았다. 목록이 바뀔 때마다 다시 풀되, **이번 실행에서 이미 물어본 id는
      다시 묻지 않아**(`askedLinks`) 새로고침마다 요청이 늘지 않는다. 답이 없던 id는 하루 한 번 —
      일일 체크가 도는 그 순간 — 다시 묻는다(어제 심사 중이던 앱이 오늘 판매 중이다)

- [x] 실패한 읽기에 **그 실패에 맞는 안내를**(`CloudKitFailure`): 네트워크가 끊겨 못 읽은 것도
      "CKErrorDomain error 4" 뒤에 "스키마가 배포됐고 admin 에 read 권한이 있는지 확인하세요"가
      붙어 나왔다 — 끊긴 연결에 대해 잘못된 화면을 가리킨다. 에러를 한 곳에서 번역하고(피드백·통계
      두 경로가 같은 표를 쓴다) **무슨 일이 있었나**와 **뭘 해야 하나**를 갈라, 안내는 실패 종류마다
      한 줄씩만 맨 밑에 모은다(같은 안내가 타입 수만큼 반복되면 문제가 여러 개로 읽힌다)
- [x] 통계를 **전체 · 유료 · 무료**로 갈라 보기(`FeedbackStore+Audience.swift`): 앱이 보낸 0/1
      플래그로 설치를 나누고 대시보드 전체를 그 무리만 놓고 다시 그린다(타일·DAU/WAU/MAU·성장 상한·
      앱별 스펙·지표 평균·분포·사용 내역). 사람·설치 수는 집합 교집합이라 정확하고, **사용 건수는
      가르지 않고 감춘다** — 일 버킷은 그날의 건수와 활동 설치 집합만 들고 있어 설치별 건수가 없다.
      유료 여부를 안 보내는 앱의 설치는 어느 쪽에도 넣지 않고(유료+무료 < 전체), 얼마를 덮고 있는지
      고르개 밑에 적는다. 플래그를 보내는 앱이 없으면 고르개 자체가 안 뜬다
- [x] 프로젝트 카드·사이드바·부제를 **절대값에서 변화로**(`TrendFigure`): "7일 사용 5,000건"은
      그 자체로 결정을 못 만든다. 큰 활자는 어제↔그제 · 이번 7일↔지난 7일로 견준 결과(사용·사용자·
      신규)이고, 그 값을 만든 두 숫자는 밑줄로, 절대값(설치·피드백)은 카드 맨 밑으로. 오늘은 아직
      끝나지 않은 하루라 안 센다. 앞 창이 0이면 비율 대신 "처음"
- [x] 성장 상한을 프로젝트 카드·사이드바에도(`CapacityGlance`): 변화 넷 아래 한 줄로 "지금 N명 ·
      상한 M명 (71%)" + 막대. 상한은 변화가 아니라 위치라 화살표를 안 붙이고, 넘어선 상태는
      퍼센트 대신 "넘어섬"으로 — 248%는 좋은 소식처럼 읽히지만 못 떠받치는 수라는 뜻이다
- [x] 안 읽은/확인이 필요한 피드백을 **뱃지로**: 프로젝트 카드 제목 줄, 사이드바 행,
      프로젝트 화면의 피드백 버튼. 숫자 칸에 섞여 있으면 "읽을 것"과 "구경할 것"이 같은 무게로 보인다
- [x] 차트에서 가리킨 자리의 정확한 값(`ChartReadout`): 눈금 사이의 점은 "13쯤"까지만 읽힌다.
      맥은 마우스를 올리면, 아이폰은 손가락을 대고 밀면 세로선이 서고 그 자리의 모든(켜진)
      계열 값이 숫자로 뜬다. 활성 사용자·기간별 추이·성장 상한 세 차트 모두.
      성장 상한은 활동 사용자와 함께 그 기간의 이탈률·신규까지 적는다
- [x] 겹쳐 그린 차트의 범례를 스위치로(`ChartLegend`): 축을 공유하면 가장 큰 계열이 눈금을
      정해 작은 계열이 바닥에 눌린다(MAU 42인 축에서 DAU 0~11). 계열을 끄면 축이 켜진 것에만
      맞춰 다시 잡혀 같은 그래프가 DAU 한 계열의 그래프가 된다. 활성 사용자·기간별 추이·
      성장 상한 세 차트에 모두. 마지막 하나는 안 꺼지고, 우클릭에 "○○만 보기"
- [x] 읽음·처리 상태를 iCloud로 기기 사이에 하나로(`ReviewStateSync`, 키·값 저장소):
      레코드는 앱 것이고 뷰어는 읽기 권한뿐이라 판단을 레코드에 못 적는다. 읽음은 합집합,
      처리는 `decidedAt`이 늦은 쪽이 이기게 합치고, 되돌리기를 전하려고 빈 항목도 시각과 함께 남긴다.
      로그인이 없으면 지금까지처럼 기기 안에서만 — 그 사실은 "내 계정 ID" 메뉴가 한 줄로 알린다
- [x] 활성 유료·무료 사용자: 앱이 보낸 0/1 플래그(스펙의 `paidFlag`, 없으면 흔한 이름 추측)로
      전체·최근 7일·최근 30일을 가른다. 활성 기준을 위 타일과 같은 것(스냅샷 `lastActiveAt`)으로
      맞춰 유료+무료가 타일 숫자와 정확히 일치. 결제 영수증이 아니라는 사실을 각주에 그대로
- [x] 성장 상한에 일간(DAU) 칸 추가 → 상한이 DAU·WAU·MAU 각각의 평형이 됨. 하루 칸은 "어제 쓰고
      오늘 안 씀"이 전부 이탈로 잡히므로 그 사실을 주의로 적는다
- [x] 통계 화면의 틀을 앱마다 동일하게: 데이터가 없어도 카드가 사라지지 않고 같은 자리에 뜨며,
      빈칸 대신 "왜 비었는지"가 들어간다(아직 안 보내는 것과 0인 것은 다른 말이다)
- [x] iOS 섹션 버튼 글자가 배경 밖으로 삐져나와 맨 끝 칸이 화면 밖으로 잘리던 것 수정:
      두 줄 모두에 `frame(maxWidth: .infinity)`를 줘 칸 너비를 제안받게 하고, 안쪽 여백과
      더 작은 최소 축소율을 더함
- [x] 통계에 일간·주간·월간 활성 사용자(DAU·WAU·MAU): 같은 것을 세 창으로 잰 값이라
      더하지 않고 언제나 일 버킷 installID 집합의 합집합. 앞의 같은 길이 창과 견준 화살표,
      고착도(DAU÷MAU = 30일 중 며칠 오는가), 하루씩 물러나며 다시 잰 30일 추이까지 한 카드에.
      월간은 달력의 달이 아니라 30일 고정(창 길이가 달라지면 사용자가 아니라 달력이 변한 것)
- [x] 성장 상한(carrying capacity): 일 버킷의 installID 집합에서 기간당 신규·이탈률을 재고
      `신규 ÷ 이탈률`로 활동 사용자의 평형을 계산. 상한 대비 지금 자리, 평균 사용 수명,
      실제 궤적 + 전망선 + 상한선, 그리고 표본 부족·활동 0 기간 같은 흔들림을 카드에 그대로 표시
- [x] 통계 집계를 새로고침마다 한 번만 계산(`FeedbackStore.Derived`): 프로젝트별 레코드 묶음·트래픽·
      요약·진단 롤업·이벤트/지표/추이를 캐시하고, 받아온 레코드·숨김·읽음/확인·앱 이름이 바뀔 때만 폐기.
      카드마다 전체 레코드를 다시 훑던 O(프로젝트 수 × 레코드 수) 렌더 비용 제거
- [x] 앱별 통계 스펙(JSON): 각 앱 리포의 docs/usage-spec.json → scripts/sync-stats-specs.sh →
      뷰어 Specs/. ClipKeyboard·두번알림의 라벨·핵심 지표·분포·세그먼트를 앱 화면 그대로 재현
- [x] 스펙에 없는 지표·이벤트를 통계 화면에 모아 알림(드리프트를 눈에 보이게)
- [x] macOS 사이드바에도 14일 사용량 스파크라인(어느 프로젝트가 얼마나 쓰이는지)
- [x] "최근 7일 사용 N건"을 실제로 확인할 수 있는 사용 내역(개별 이벤트) 카드
- [x] 통계 화면에서 크래시·진단 카드 제거(진단 세그먼트와 중복)
- [x] 프로젝트 목록을 트래픽(최근 7일 사용 건수) 많은 순으로 정렬 + 그 숫자를 카드·사이드바에 표시
- [x] 진단을 앱 버전별로 묶어서 표시(최신 버전 위, 헤더에 전체·최근 7일 건수)
- [x] 사용 통계·진단만 올린 앱도 프로젝트 목록에 표시
- [x] 큼직하고 직관적인 UI 패스: 숫자마다 이름 붙이기(빨강3/파랑3 뱃지 제거), 세 섹션을 큰 버튼으로,
      목록 각 줄에 보이는 "반영함" 동그라미, 검색은 버튼을 눌렀을 때만 등장
- [x] 피드백 처리(반영함/반영 안 함) + 메모 → 목록에서 감추기, 기기에 저장(`feedbackTriage`)
      (읽음과 별개. 행 동그라미·스와이프·우클릭·상세 "처리" 칸에서 지정,
      목록 아래 "처리한 피드백 N건 보기"로 다시 봄)
- [x] 화면 위계 정리: 프로젝트가 최상위, 그 안에서 피드백·통계·진단 세그먼트 전환
      (iPhone 탭 바 제거, macOS 사이드바는 프로젝트 목록 전용, 필터는 목록 옵션 메뉴로)
- [x] 마지막 조회 결과를 기기에 저장(`FeedbackCache`, 환경별 JSON) → 실행 즉시 화면 표시
- [x] 실행 시 업데이트 확인은 스토어가 소유한 별도 태스크로 분리(화면이 기다리지 않음)
- [x] 레코드 타입별 증분 조회(마지막 조회 시각 이후 변경분) + 레코드명 기준 병합
- [x] 기기에 없는 데이터만 조회: 최신순으로 읽다가 이미 저장된 레코드를 만나면 페이지 중단
      (추가만 되는 Feedback·UsageEvent·CrashReport. UsageSnapshot은 덮어쓰기라 제외)
- [x] 스냅샷은 매번 전체를 읽으므로 내용 지문 비교로 불필요한 캐시 재작성 방지
- [x] 시스템 타임스탬프가 Queryable이 아니면 전체 조회로 자동 폴백, 그 결과를 캐시에 기억
- [x] 24시간 지난 캐시·수동 새로고침은 전체 재조회(콘솔에서 삭제된 레코드 반영)
- [x] "캐시 비우고 전체 다시 불러오기" 메뉴
- [x] 이벤트를 도착 시점에 일 단위로 합산(`UsageRollups`) → `rollups-<환경>.json`으로 분리 저장.
      주/월/연은 일 버킷 합, 활동 사용자는 일별 installID 집합의 합집합. 합산은 몇 번 해도 같고
      (날짜마다 이미 센 레코드명 보관, 180일 뒤 정리), 로우 이벤트는 최근 90일치만 남김.
      화면의 모든 사용량 숫자가 롤업에서 나오므로 비용이 이벤트 수가 아니라 하루 수에 비례
- [x] `UsageEvent`는 전체 조회에서도 이미 가진 것에서 멈춤(합산된 날을 되돌릴 수 없으므로
      전체 재조회가 가장 비싸고 얻는 게 없음. 재구축은 "캐시 비우고 전체 다시 불러오기")
- [x] 조회 도중에도 500건마다·타입이 끝날 때마다 캐시 저장, 종료·백그라운드 진입 시 남은 것 즉시 기록.
      (예전에는 네 종류를 다 읽은 뒤 한 번만 저장해서, 첫 실행 도중 앱을 끄면 받은 게 전부 날아갔음.
      중간에 끊긴 타입은 워터마크가 없으므로 다음 실행에서 처음부터 다시 읽음)
- [x] 맥 툴바에서 자동 갱신 아이콘을 ⏱(`timer`)로 — 새로고침(↻)과 같은 원형 화살표라 구분이 안 됐음
- [x] 무효화 세분화: 읽음·확인 → 안 읽음 수만, 앱 이름 → 이름 쓰는 것만, 새 이벤트 → 사용량만.
      자동 갱신이 못 찾았으면 대입 자체를 생략(같은 값 대입도 `didSet`으로 집계를 통째로 버림)

- [x] 앱별 결제 퍼널(`funnels` 절): 이벤트 기본형으로 단계를 적고 슬라이스는 합침, 기본은 설치 수
      (합집합이라 중복 안 됨). 전 기간 롤업에서 바로 나와 원본을 안 훑음.
      "보내지 않음"(이벤트 미전송)과 "앞 단계 밖에서도 옴"(포함 관계 아님)을 화면에 드러냄
- [x] 두번알림 스펙에 빠진 이벤트 라벨 5개 보충
      (onboarding_shown · timer_cancelled · preset_saved · purchase_started · purchase_failed)
- [x] sync-stats-specs.sh의 ClipKeyboard 경로 수정 (docs/ → docs/engineering/)
- [x] 일/주/월/연 요약 사다리(`UsageRollups.weeks/months/years`): 일 버킷이 진실, 그 위 단계는 일의 합.
      새로고침이 건드린 날의 기간만 다시 합산(`dirtyDays` → `rebuildDirtyPeriods`)하므로 갱신 비용이
      쌓인 양이 아니라 바뀐 양에 비례. 주/월/연 추이가 전 기간 일 버킷을 다시 훑지 않고 버킷 하나를 읽음.
      v1 파일은 버리지 않고 일 버킷에서 사다리를 다시 세워 올림(지역 변경으로 주 경계가 바뀌어도 동일)
- [x] 이벤트·진단 조회의 5,000/1,000건 상한 제거: 이어서 읽을 방법이 없는 읽기에 상한을 걸면
      "최신 N건"이 아니라 "아무 N건"이 돌아오는데, 그 읽기를 끝까지 읽은 것으로 기록해서
      나머지를 영영 다시 묻지 않았음. 보관 상한(`withinRetention`·`crashLimit`)만 남김
- [x] 정렬도 필터도 안 되는 레코드 타입을 화면에 알림(`unindexedTypes`) + 캐시에 기록.
      지금 이 컨테이너에서는 `UsageSnapshot`이 그래서 새로고침마다 4,600건 전체를 읽고 있음

- [x] **크래시 이슈 분석**: 같은 사고끼리 묶어 아픈 것부터(`CrashIssue`). 원인 분류
      (워치독 종료 지연·화면 갱신 지연·신호·멈춤·메모리), 첫 등장/마지막, 최근 7일과 지난주 대비,
      버전·기기·OS 분포, 내 코드 프레임을 굵게 그린 콜스택, 새로 생김/잠잠 표시,
      그리고 dSYM 을 찾아 `atos` 를 돌리는 명령 복사까지. 진단 화면은 이슈/전체 두 시야
- [x] 보내는 쪽(ClipKeyboard 5.0.6)에서 콜스택을 프레임 목록으로 바꿈. 그 전 149건은
      JSON 중간에서 잘려 분석 불가라 "옛 형식" 한 덩어리로 모으고 이유를 화면에 적음
- [x] **ASO(키워드) 섹션**: 프로젝트 안에 피드백·통계·진단과 나란히 "키워드" 추가.
      iTunes Search API로 App Store 검색 순위를 매일 추적하고, 검색 결과 상위에서 경쟁 앱을 수집.
      번들 ID → trackId 자동 연결(`lookup?bundleId=`, 20개씩 묶어 한 번에).
      설계의 핵심: **검색 1회가 내 앱 전부를 커버**하므로 요청 비용이 `키워드 × 국가`(앱 수와 무관).
      순위 이력은 일 단위 1건이라 사다리 없이 일 버킷만 둠(합이 아니라 시점 값이라 정의도 다름).
      요청은 3초 간격으로 직렬화, 하루 한 번만 확인, 화면에 검색량 없음·정렬 근사·Play 미지원을 명시

## 남은 것
- [ ] (선택) 경쟁 앱이 랭킹된 키워드 역추적 — "저쪽은 있고 나는 없는" 키워드 발굴.
      요청이 여러 배로 늘어나므로 탐색 전략(어떤 후보를 어떤 순서로 볼지)이 먼저 필요
- [ ] (선택) AI 리스팅 제안 — 순위·경쟁 앱 데이터를 근거로 앱 이름·설명 개선안. Claude API 키·비용 발생
- [ ] Google Play 순위 — 공식 API가 없어 스크래핑이 필요. App Store와는 다른 종류의 작업
- [ ] **CloudKit 인덱스 추가**: 네 타입 모두 `___createTime`/`___modTime`이 Queryable이 아니라
      증분 조회가 한 번도 동작한 적 없음(캐시의 `filterFields`가 전부 빈 값). UsageEvent·CrashReport·
      Feedback은 정렬이 되어 "이미 가진 것에서 멈추기"로 버티지만, UsageSnapshot은 정렬도 안 돼
      매번 전체를 읽음. Console → 각 레코드 타입 Indexes에서 Created·Modified를 Queryable·Sortable로
- [ ] **앱스토어 설치 수 ≫ 허브 설치 수**: 허브의 "설치 수"는 UsageSnapshot 레코드 수 = *리포트가
      도달한* 설치 수. 조회 쪽은 완전함을 확인했으므로(스냅샷은 상한 없는 전수 조회) 간극은 앱 쪽
      전송 유실. LeeoKit 리포터에서 확인할 것: iCloud 미로그인 시 동작, 첫 실행 직후 종료 시 저장 완료,
      CKError 재시도, 공개 DB 쓰기 스로틀링
- [ ] **두번알림에 결제 성공 이벤트가 없음**: `purchase_started`·`purchase_failed`는 오는데 완료가
      안 옴(스펙의 `paywall_converted`가 "보내지 않음"으로 뜸). 앱에서 보내야 퍼널 마지막 칸이 채워짐
- [ ] (선택) ClipKeyboard `paywall_view`에 출처 슬라이스(`:nudge_slots_left` 등)를 붙이면
      Pro 넛지 퍼널을 결제까지 이을 수 있음. 지금은 포함 관계가 아니라 2단계에서 끊어 둠
- [ ] `com.Ysoup.LeaveWise`가 `paywall_view`를 보내는데 스펙이 없음 — 붙일지 결정
- [ ] **앱 리포 스펙에 `accessFlag` 선언**: ClipKeyboard·두번알림 모두 지금은 `flag.isPro`를
      이름만 보고 접근 키로 고르고 있다(추측이라 각주에 그렇게 적힌다). 각 앱 리포의
      `docs/usage-spec.json`에 `"accessFlag": "flag.isPro"` 한 줄 → `sync-stats-specs.sh`.
      뷰어 쪽 `Specs/` 복사본을 직접 고치면 `--check`가 깨지므로 원본부터 고칠 것
- [ ] 새 앱을 붙일 때: 그 앱 리포에 docs/usage-spec.json 추가 → sync-stats-specs.sh의 SPECS에 한 줄
- [ ] (선택) 커밋 전 `./scripts/sync-stats-specs.sh --check`를 훅/CI에 걸어 스펙 드리프트 차단
- [ ] (선택) 리텐션·코호트까지 스펙으로 표현(퍼널까지는 됨)
- [ ] (선택) CloudKit Console에서 `createdTimestamp`(Feedback·UsageEvent·CrashReport) /
      `modifiedTimestamp`(UsageSnapshot)를 QUERYABLE 인덱스로 추가 → 서버 쪽 날짜 필터까지 사용
      (없어도 새 레코드만 받습니다. README §5-7 참고, 코드 변경 없이 앱이 자동 감지)
- [ ] 앱이 꺼져 있을 때 알림: Push Notifications capability + `CKQuerySubscription`(미구현)
- [ ] **다른 앱들도 5.0.6 의 진단 전송으로 올리기**: 지금은 ClipKeyboard 만 새 형식이다.
      `DiagnosticsService` 를 LeeoKit 으로 올려 46개 앱이 같은 형식을 쓰게 하면
      이 화면이 그대로 전 포트폴리오에 적용된다
- [ ] (선택) dSYM 보관 자동화: 아카이브에서 `dSYMs/` 를 따로 떠 두는 스크립트.
      지금은 손으로 챙겨야 하고, 아카이브를 지우면 그 빌드는 영영 심볼을 못 붙인다
- [ ] (선택) 이슈 "확인함" 표시: 피드백처럼 이미 본 이슈를 접어 두기
- [ ] (선택) 크래시율(크래시 ÷ 활동 사용자): 지금은 절대 건수만 보여서, 사용자가 늘어
      건수가 는 것과 실제로 나빠진 것이 구분되지 않는다. 롤업의 일별 활동 사용자와 나누면 된다
